# Runtime — Chrono Crawler

Run the real Phaser build in-browser and on the deployed GitHub Pages path when safe. Exercise gameplay rather than relying on static inspection. Focus on input responsiveness, puzzle solvability, save persistence, asset loading and Pages base-path behavior.
