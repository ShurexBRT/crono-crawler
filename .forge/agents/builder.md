# Builder — Chrono Crawler

Implement approved scoped work without silently expanding the game. Preserve timeline-shift, echo, checkpoint/save and puzzle semantics. Use existing Phaser/TypeScript architecture. Do not turn TODO wishes into features unless a ticket is product-approved. Handoff to Reviewer.
